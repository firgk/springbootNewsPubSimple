<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<!-- src/main/resources/mapper/CommentMapper.xml -->

<mapper namespace="com.concat.mapper.CommentMapper">

    <resultMap id="commentResultMap" type="com.concat.domain.Comment">
        <id property="commentId" column="comment_id"/>
        <result property="userId" column="user_id"/>
        <result property="newsId" column="news_id"/>
        <result property="content" column="content"/>
        <result property="createdAt" column="created_at"/>
    </resultMap>

    <select id="getCommentById" resultMap="commentResultMap">
        SELECT * FROM Comment WHERE comment_id = #{commentId}
    </select>

    <insert id="insertComment">
        INSERT INTO Comment (user_id, news_id, content)
        VALUES (#{userId}, #{newsId}, #{content})
    </insert>

    <update id="updateComment">
        UPDATE Comment SET user_id = #{userId}, news_id = #{newsId},
        content = #{content} WHERE comment_id = #{commentId}
    </update>

    <delete id="deleteCommentById">
        DELETE FROM Comment WHERE comment_id = #{commentId}
    </delete>

    <select id="getCommentsByNewsId" resultMap="commentResultMap">
        SELECT * FROM Comment WHERE news_id = #{newsId}
    </select>

    <delete id="deleteCommentByNewsId">
        DELETE FROM Comment WHERE news_id = #{newsId}
    </delete>


    <delete id="deleteCommentByUserId">
        DELETE FROM Comment WHERE user_id = #{userId}
    </delete>

</mapper>
