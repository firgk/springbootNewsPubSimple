<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<!-- src/main/resources/mapper/CategoryMapper.xml -->

<mapper namespace="com.concat.mapper.CategoryMapper">

    <resultMap id="categoryResultMap" type="com.concat.domain.Category">
        <id property="categoryId" column="category_id"/>
        <result property="name" column="name"/>
    </resultMap>

    <select id="getCategoryById" resultMap="categoryResultMap">
        SELECT * FROM Category WHERE category_id = #{categoryId}
    </select>

    <insert id="insertCategory">
        INSERT INTO Category (name) VALUES (#{name})
    </insert>

    <update id="updateCategory">
        UPDATE Category SET name = #{name} WHERE category_id = #{categoryId}
    </update>

    <delete id="deleteCategoryById">
        DELETE FROM Category WHERE category_id = #{categoryId}
    </delete>

    <select id="getAllCategories" resultMap="categoryResultMap">
        SELECT * FROM Category
    </select>

    <!-- 可根据需要添加其他查询方法 -->

</mapper>
